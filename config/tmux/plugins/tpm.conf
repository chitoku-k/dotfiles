# vim: set ft=tmux :

set-environment -g TMUX_PLUGIN_MANAGER_PATH $XDG_CACHE_HOME/tpm/plugins
set -g @tpm_plugins ' \
    tmux-plugins/tpm \
    dastergon/tmux-load-avg \
'

run $XDG_CACHE_HOME/tpm/plugins/tpm
